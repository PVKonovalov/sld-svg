# RELEASE.md

* 2026-08-19: Implemented the `svg-text` CLI (`extract`/`inject` subcommands) for translating text embedded in single-line-diagram SVG files — `data-name="..."` attributes and `<text>`/`<tspan>` content — using byte-exact XML scanning and patching so untranslated parts of a file are never altered.
* 2026-08-19: Added a per-SVG, semicolon-separated `key;source;translation` CSV translation-file format, keyed by a content hash so files can be freely reordered and re-extracted without losing existing translations.
* 2026-08-19: Added `--ru-en` (on `extract`) and `--filename-en` (on `inject`) flags providing a plain Cyrillic-to-Latin transliteration to pre-fill translations/filenames.
* 2026-08-19: Added `-dictionary` flag on `extract` plus a tracked `dictionary.csv` of common electrical units/abbreviations, to pre-fill exact, case-insensitive whole-string matches ahead of `--ru-en`.
* 2026-08-19: Added the `null` sentinel translation value (case-insensitive) meaning "delete this occurrence" (replace with an empty string), distinct from a blank translation ("not yet translated, leave untouched"); mapped the live-clock placeholder strings (`DD.MM.YYYY`, `DD:MM:YYYY HH:MM:SS`, and the typo'd `DD:MM:YYY HH:MM:SS` variant) in `dictionary.csv` to `null` so they're stripped from translated output automatically.
* 2026-08-19: Added `-rewrite` flag on `extract` to force-regenerate every translation from `-dictionary`/`--ru-en` instead of preserving whatever's already in an existing output `.csv` — needed because dictionary improvements couldn't otherwise reach files that were already fully filled by an earlier `--ru-en` run. Used it to regenerate all 144 `strings/*.csv` and `translated/*.svg` files against the current dictionary.
* 2026-08-19: Made dictionary matching whitespace-insensitive (spaces dropped entirely, not just collapsed, before comparing) so entries like `1СШ 10кВ` also match real source strings formatted with different spacing, e.g. `1 сш 10 кВ`. Regenerated `strings/*.csv` and `translated/*.svg` again to pick up the newly-matching entries.
* 2026-08-20: Added a new `internal/slddoc` package and `cmd/svg-sld` CLI (`extract`/`render` subcommands) that models a single-line diagram as a standalone XML document — placed elements with coordinates/orientation, drawn connectors, and an electrical topology (a node graph) reconstructed from the SVG's own geometry — separate from `svgtext`. `extract` covers a representative subset of xsde2svg's equipment catalog: busbars, generic wires (ошиновка/воздушная_линия/кабельная_линия/связь_с_объектом), junction points (точка), breakers (fixed and withdrawable), disconnectors, ground switches, and 2-winding power transformers; anything else is reported as skipped rather than silently dropped or guessed at. Element port geometry (where each symbol's electrical terminals are, before rotation) was derived directly from the corresponding `element_<code>.go` rendering function in the sibling `xsde2svg` source project, not reverse-engineered from rendered output. Topology is reconstructed by merging exact-coincident coordinates (wire endpoints, `точка` dots, resolved element ports) via union-find, with a small-tolerance snap pass for the few-pixel gaps some symbols' formulas leave, and by attaching anything that lies along a busbar's drawn length (not just its endpoints) to that busbar's single electrical node. `render` reads such an XML document back into a fresh SVG using a new tracked `symbols.xml` (one local-coordinate template per xsde2svg shape code, keyed separately from the semantic equipment class since two shapes, e.g. a fixed and a withdrawable breaker, can share a class) — it does not aim for byte-identical reproduction of the original SVG the way `svg-text inject` does, only a faithful new rendering. Also added a tracked `voltage-hints.csv` (hex color → real voltage-class name, e.g. `#962896;10кВ`) that `extract` uses to pre-fill each diagram's own `<voltageClasses>` table — voltage is modeled as this logical class name, not a raw color, with color kept only as that class's display style for rendering. Every element/connector/label also carries a mandatory `layer` reference (into a per-diagram `<layers>` table carried over from the SVG's `<metadata>`, plus an implicit always-present base layer), so a downstream viewer can show/hide by layer.
* 2026-08-20: Added a `## svg-sld` section to README.md documenting `extract`/`render` usage, an example diagram XML, and how `class`/`shape`/`voltage`/`layer` relate.
* 2026-08-20: Expanded `svg-sld extract`'s equipment coverage from 7 to 15 xsde2svg shapes, adding load-break switches (42), withdrawable disconnectors (49), ground terminals (31), choke coils (33), current transformers (34), surge arresters (35), fuses (203), and capacitors (388) — each verified against real instances from the example corpus, not guessed. Replaced the breaker/disconnector-specific port parsers with one generic `parseTwoPortDevice`, after finding that every one of these two-terminal shapes' true ports are always the pair of points furthest apart along the device's dominant axis across all of its path geometry, regardless of which shape-specific subpath convention produces them — a simpler and more robust rule than the per-shape "start point vs. end point" special-casing the original breaker/disconnector parsers used. Added minimal SVG arc (`A`/`a`) support to the path interpreter (endpoint tracking only) to support choke coils and current transformers, both of which draw their coil/loop geometry with arcs. Added corresponding `symbols.xml` render templates for all eight new shapes.
* 2026-08-20: Fixed the withdrawable breaker's (shape 43) `symbols.xml` render template, which had dropped a real path when first transcribed: the withdrawable "trolley" isolating contact is drawn as a short stub from the box plus a chevron on each side, with a small gap out to the fixed contact's own chevron — that gap is the isolating throw distance, by design, not a missing connector, but without the inner stub+chevron the render showed only the outer chevron floating with a large empty gap.
* 2026-08-20: Made the switch-like devices' (Breaker: 41/43; LoadBreakSwitch: 42; Disconnector: 71/162) internal state indicator actually reflect Element.State, instead of always drawing the same fixed line regardless of state: parallel to the device's own axis when closed, perpendicular when open (both confirmed against the xsde2svg source, which draws a vertical vs. horizontal line the same way for every one of these shapes), and diagonal (45°) for any other recorded value — a live-status convention supplied by the user that this static export format doesn't itself encode. Added a `{state:parallel|perpendicular|diagonal}` template placeholder in `symbols.xml` to select between the three per-shape variants at render time.
* 2026-08-21: Made `Diagram.Save` (`internal/slddoc/model.go`) emit self-closing tags (`<node .../>`) for attribute-only/empty elements instead of `encoding/xml`'s default `<node ...></node>`, via a post-processing regex pass over the encoded output — `Load` round-trips either form unchanged.
* 2026-08-22: Fixed `parseTwoPortDevice` (`internal/slddoc/elements.go`) ignoring a rotate() transform placed on an inner wrapping `<g>` around a two-port device's `<path>`s instead of on the element's own top-level tag — a pattern `Examples.svg` uses for rotated breakers (41) and disconnectors (162), e.g. element 1030 (`rotate(-90,2230,520)`), 2507, and 959, which were all extracting/rendering unrotated. Now searches the element and its descendants for the transform, not just the element itself.
* 2026-08-22: Added shape 106 (лампа/Lamp status indicator) to `svg-sld extract`/`render`. Verified against the xsde2svg source that shape 5 (initially proposed as "lamp") is actually `Текст`/plain text — this project's existing generic caption mechanism, already correctly routed to `Labels` — while shape 106 (`// element106 Лампа`) is the real lamp: a bare `<circle>` with no electrical port (real instances are never a wire's endpoint, so it's excluded from topology), whose `data-fill="0:off,1:on"` colors are read directly into new `Element.FillOff`/`FillOn` fields and picked between by `State` at render time, rather than reduced to the switch-like devices' fixed red/lawngreen/yellow state-indicator convention — since a lamp's colors are its entire appearance and carry real per-instance meaning. Added a `symbols.xml` template (a plain circle, `r="11"` matching the corpus's most common radius, `stroke:slategray` matching the source styling).
* 2026-08-22: Fixed the Lamp radius (previous entry) being hardcoded to `r="11"` in `symbols.xml` instead of read per instance — real corpus instances vary meaningfully, e.g. `Examples.svg` draws `r=11` "Индикатор" panel lights vs. `r=5` "АВ Валдай" lamps clustered in triplets next to a breaker. Added `Element.Radius` (from the circle's own `r` attribute) and a `{radius}` template placeholder so each instance renders at its real recorded size.
* 2026-08-22: Made `svg-sld render`'s output annotate each contiguous run of same-type elements/connectors with an English `<!-- Name -->` (elements: `<!-- Name:shape -->`) comment header, mirroring the source SVG's own `<!-- <russian_name>:<code> -->` convention (e.g. `<!-- разъединитель:162 -->` in `Examples.svg`) — translated, and keyed per-shape/per-connector-kind rather than per logical `Class`, since the source itself distinguishes shapes sharing one `Class` (e.g. a fixed vs. withdrawable breaker, both `ClassBreaker`, get separate headers "Breaker:41"/"Breaker (withdrawable):43").
* 2026-08-24: Added shape 320003 (ИКЗ/Fault Passage Indicator, "FPI") to `svg-sld extract`/`render`, a new `ClassFaultPassageIndicator`. Shape 320 is xsde2svg's generic "custom element" code covering several unrelated sub-variants keyed by `sde.InText` (direction arrows, pole lamps, ИКЗ, ...), of which only ИКЗ (rendered as `data-type="320003"`) is supported; real instances draw a `<g>` wrapping a fixed circle plus state-dependent decorative graphics (an "FPI" text label or arrow icons), so only the circle's position/radius are extracted, and — like the existing Lamp status indicator — it carries no electrical port and is excluded from topology. Its fill is rendered from the fixed `defaultBackground` constant rather than a `VoltageClass`, since every real instance draws the same dark fill regardless of state. Added the `symbols.xml` template (user-supplied, keyed `shape="320003"` to match the real `data-type`) and fixed its `<text>` element's blank `x`/`y` attributes to `"0"`/`"0"` so the "FPI" label actually centers on the circle.
* 2026-08-24: Fixed `svg-sld render` drawing every FaultPassageIndicator before the connectors, so a wire passing through one's position painted over and cut through its opaque circle. Unlike every other Element, a FaultPassageIndicator sits directly on top of a wire rather than only touching one at a port, so `Render` now defers it past the connectors loop instead of drawing it in document order — the wire is painted first, then the circle fully covers it, matching how the source SVG actually looks.
* 2026-08-24: Generalized the fix above into a proper Z-order setting (`elementZOrder` in `internal/slddoc/render.go`) instead of a single FaultPassageIndicator-only special case, and extended it to JunctionPoint (also drawn directly on top of a wire, same problem) and Lamp (a decorative status indicator meant to read as foreground UI). `Render` now buckets any Class with a nonzero entry into ascending tiers drawn after the connectors loop, so each still renders above every connector regardless of its position in the source document, while labels remain last (already on top of everything) and every other Class keeps rendering in ordinary document order, before connectors, as before.
* 2026-09-07: Added `IEEE9bus.svg` at the repo root, a hand-authored sample SLD of the IEEE 9-bus test system (from `Sample-test-system-the-IEEE-9-bus-grid.png`) on a dark background, following this project's SVG conventions (the `#12161d` background `internal/slddoc/render.go` already uses, `<g id data-type data-name>` grouped elements, per-element voltage/role coloring, plain `<text>` labels in Arial). Generator, load, and transmission-line-box symbols aren't part of xsde2svg's numeric shape catalog, so they're drawn as one-off shapes with descriptive (non-numeric) `data-type` values rather than going through `internal/slddoc`/`symbols.xml`, which has no Generator/Load class yet.
* 2026-09-07: Reworked `IEEE9bus.svg` to match real xsde2svg conventions more closely, cross-checked against `examples/sld/PS_35kV_Krasnaya.svg` and `docs/SLD_SVG_ELEMENTS.md`'s type catalog rather than inventing ids/codes: every element `id` is now a plain integer (unique per file, per the attribute reference) instead of a descriptive string; the transmission-line, generator, and load shapes now carry real catalog `data-type` codes (`22` Overhead line, `173` Generator, `28` Object link — the catalog has no dedicated load/consumer shape) instead of made-up non-numeric values; and every Bus/Generator/Object-link/Overhead-line element now carries a `data-voltage` attribute (the bus's own fill color for `Bus:24`, the stroke color for the other three), matching the real corpus's per-element voltage/color attribute.
* 2026-09-07: Fixed `IEEE9bus.svg`'s Line1/Line2 (the two branches meeting at Bus 8): they used to route through an anonymous, unlabeled junction wire dropping straight down to Bus 8, with both lines' vertical segments overlapping at the exact same x — rendering as what looked like one single line, and leaving Bus 8 connected via a stub that belonged to neither labeled line. Each of Line1/Line2 is now one self-contained bent (L-shaped) `Overhead line:22` polyline running directly from its own bus (Bus 7 / Bus 9) to Bus 8, with the two verticals offset a few pixels apart so they read as two distinct lines rather than one merged stroke.
* 2026-09-07: Replaced every inline connection-dot `<circle>` in `IEEE9bus.svg`'s Generator/Object-link/Overhead-line groups (24 total, one deduped pair sharing a physical node) with a proper `Point:7` element — a bare `<circle data-type="7">` with no wrapping `<g>`, `fill` = background color and `stroke`/`data-voltage` = the connected wire's color, matching `examples/sld/PS_35kV_Krasnaya.svg`'s own Point convention — all grouped in one `<!-- Point:7 -->` block placed after every other element so the dots draw on top of the whole diagram. Also added the three Object-link (load) elements' own previously-missing bus-attachment points to that block.
* 2026-09-07: Moved every freestanding caption in `IEEE9bus.svg` (9 bus names, 3 generator MW values, 3 load MW values, 9 line names — 24 total) out of their element groups into a new `<!-- Text:5 -->` block, each as a bare `<text data-type="5">` with no wrapping `<g>`, placed right after `<!-- Point:7 -->` so captions draw on top too — matching the source corpus's own convention of pulling equipment-name/value captions out as standalone `data-type="5"` elements rather than nesting them under the equipment they label. A shape's own baked-in pictogram text (the generator's "P V", the line box's "π π") stayed put, since it's part of that symbol's own drawing, not a freestanding caption — the same distinction the FPI symbol's baked-in "FPI" text already draws in `symbols.xml`.
* 2026-09-07: Changed `IEEE9bus.svg`'s `Bus:24` elements from filled `<rect>`s to `<polyline style="fill:none;stroke:{color};stroke-width:4">`, matching `internal/slddoc/render.go`'s own `writePolyline` convention for `BusBarSection` (just a thicker stroke-width, 4 instead of 1, to keep the bus bars visually prominent against the dark background).
* 2026-09-08: Added `WSCC9.svg` at the repo root, a hand-authored sample SLD of the WSCC 9-bus test system (from `WSCC9.png`) — the same underlying topology as `IEEE9bus.svg`, built the same way, and reusing that file's exact element ids for every element the two share. Two structural differences from `IEEE9bus.svg`, per `WSCC9.png`: the three generator-to-network links (Bus2-Bus7, Bus3-Bus9, Bus1-Bus4 — ids 307/308/309) are 2-winding Power transformers (`data-type="47"`, named T2/T3/T1 per the standard WSCC numbering) instead of Overhead lines, drawn as two overlapping circles (a simplified pictogram, the same approach `IEEE9bus.svg` already takes for its other custom shapes, not `symbols.xml`'s full-scale template); and every red square in `WSCC9.png` is a circuit breaker (`data-type="41"`, 24 total, ids 1001-1024) — one inline on each generator's own wire, one at each end of the six pure transmission lines, one at each end of all three transformers, and one on each load's own bus tap — every one replacing what was a plain `Point:7` dot at that same coordinate in `IEEE9bus.svg`. Also added the power-flow solution `WSCC9.png` carries that `IEEE9bus.svg` doesn't (MW/Mvar per generator and load, a pu voltage magnitude per bus) as `Digital device2:134` live-readout elements per `docs/SLD_SVG_ELEMENTS.md` — bare `<text data-type="134">` with a `<tspan>` unit and a `data-unit` attribute, turquoise/bold to read as a live value distinct from the plain `Text:5` captions, matching the real corpus's own convention for this shape. The bottom generator's MW value differs from `IEEE9bus.svg` (334 vs. 72) so its name changed too (`G72` -> `G334`, same id 103); likewise the two side-branch loads (`Load 125MW` -> `Load 166MW` id 202, `Load 90MW` -> `Load 165MW` id 203). Extended the shared Legend panel with two more rows (Circuit breaker, Power transformer).
* 2026-09-08: Redrew `WSCC9.svg`'s breakers with the real xsde2svg shape-41 geometry — a 14x14 body plus a state blade plus two fixed leg stubs, matching the corpus sample the user pasted from `examples/sld/Examples.svg:430 (<g data-type="41">…)` — instead of the flat placeholder squares from the previous entry. Per `docs/SLD_SVG_ELEMENTS.md`'s Pattern A, the leg stubs always run parallel to whichever wire the breaker sits on and the state blade runs parallel when closed / perpendicular when open; every breaker here is drawn open (`data-state="0"`, red fill from its own `data-fill="0:red,1:lawngreen,2:yellow"` map) to match `WSCC9.png`'s red squares, so each one's blade is perpendicular to its wire. Also added 9 more breakers the first pass missed: one at each end of all three transformers (ids 1016-1021) and one on each load's own bus tap (ids 1022-1024), bringing the total to 24 and matching `WSCC9.png`'s actual breaker coverage more closely — the transformer and load links aren't breaker-free after all.
* 2026-09-08: Recolored every element in `WSCC9.svg` by voltage side, per the real per-element `data-voltage` convention (`docs/SLD_SVG_ELEMENTS.md`'s Attribute reference) and the transformer sample the user pasted from `examples/sld/Examples.svg:762`: `#00A0F0` from each generator up to and including its transformer's HV winding (the three generators and their wires, Bus1/Bus2/Bus3, and the six breakers closest to those buses), `#962896` for the transformer's MV winding onward — the rest of the network (Bus4/5/6/7/8/9, all six Overhead lines and their breakers, all three loads and their breakers). Rebuilt each transformer (T1/T2/T3) as two separately-colored circles, one per winding, instead of one uniform-gray pictogram — matching the cited sample's own structure, including dropping the single `data-voltage` that used to sit on the transformer's outer `<g>` (a transformer spans two voltages, so, like the real sample, only its individual windings carry `data-voltage` now).
* 2026-09-08: Scaled every breaker's and transformer's own drawn geometry in `WSCC9.svg` up by 1.5x (breaker body 14x14 -> 21x21, blade/leg lengths scaled to match; transformer winding circles r=10 -> r=15, center offset 8 -> 12) for better legibility, without moving any element's own placement coordinate — each transformer's bus-side stub wires shortened to keep meeting its now-larger winding circle's edge exactly, with no gap or overlap. Also removed the Legend panel (id 501) `IEEE9bus.svg` originally introduced and `WSCC9.svg` had been carrying forward, freeing up the bottom-right corner it occupied.
* 2026-09-08: Flipped every breaker in `WSCC9.svg` from open to closed — `data-state="0"` -> `"1"`, body fill `red` -> `lawngreen` (both per its own `data-fill="0:red,1:lawngreen,2:yellow"` map) — and rotated each one's internal state-blade segment from perpendicular to parallel with its own leg stubs, per docs/SLD_SVG_ELEMENTS.md's Pattern A (parallel = closed, matching the corpus sample at `examples/sld/Examples.svg:432-434`, which is itself closed/vertical-blade). All 24 breakers now render green.
* 2026-09-08: Fixed 7 breakers in `WSCC9.svg` that visibly touched/overlapped their neighbor after the 1.5x size increase — Bus5's and Bus6's two adjacent line-taps (only 20px apart, breaker bodies now 21px wide) and Bus8's three adjacent taps (Line2/Load100/Line1, same 20px spacing) — by moving each breaker off the crowded bus row and further along its own already-existing wire instead of changing any wire's route or endpoint: the two breakers at each bus move apart in opposite directions along their respective wires (e.g. Bus5's Line4-side breaker up toward Bus7, its Line5-side breaker down toward Bus4), leaving a clear gap between every pair with no other geometry touched.
* 2026-09-08: Fixed three more label/element overlaps in `WSCC9.svg` the 1.5x breaker/transformer resize (and the resulting breaker repositioning) had left too close together: G163's and G85's MW/Mvar readout text, which had crept into their own generator circle and (for G85) its breaker; and Bus4's name/pu-voltage labels, which the enlarged Line6-Bus4 breaker now reached into. Moved the two generators' MW/Mvar text further up, clear of both the circle and the breaker, and shifted Bus4's two labels further right, clear of the breaker — verified with a script that computes every Text:5/Digital device2:134 label's bounding box against every breaker's and generator circle's, confirming zero remaining overlaps.
* 2026-09-08: Generalized the previous ad-hoc breaker-spacing fix into a consistent rule applied to all 24 breakers in `WSCC9.svg`: no breaker sits directly on a bus bar any more — every one that still did (14 of them: both Bus7/Bus9 line and transformer taps, Bus2's and Bus3's transformer taps, all three of Bus4's taps, Bus1's transformer tap, and the two side loads' bus taps) moved a short distance off the bus and onto its own already-existing wire, the same treatment the previous entry gave the first 7. Every move stayed within that wire's current drawn length, so no polyline needed extending; verified with a script computing every breaker's bounding box against every bus segment's and every other breaker's, confirming no bus/breaker or breaker/breaker overlap remains anywhere in the file.
* 2026-09-08: Removed the pi-network box (rect + two terminal dots + "π π" glyph) from all six `Overhead line:22` elements in `WSCC9.svg`, merging each one's two wire stubs back into a single continuous `<polyline>` — a plain line with no decoration in the middle, per request. The line's own name caption (e.g. "Line4") stays in place, now just floating next to the plain wire instead of next to a symbol.
* 2026-09-08: Added a label for every one of `WSCC9.svg`'s 24 breakers, text = the breaker's own `data-name` (e.g. "CB-Line4-Bus5"), styled like the existing transformer labels (`data-type="5"`, `fill:#c8ced8;font-size:12px;font-family:Arial`). Placement follows a fixed rule — to the right for a breaker sitting on a vertical wire, above for one on a horizontal wire — enforced by a placement script that models every bus segment, generator/transformer circle, load triangle, breaker body, point, and existing label as an obstacle rectangle and searches outward from the ideal position (preferring small offsets, staying on the correct side) until it finds a spot with at least 4px clearance on every side; verified independently afterward with a second script computing every label's and breaker's bounding box pairwise, confirming zero overlaps anywhere in the file. Also nudged two breakers (Bus7's and Bus9's Line4/Line3 taps) a few pixels further down their own wire, off a pre-existing tiny overlap with the neighboring pu-voltage label that predated this pass.
* 2026-09-08: Fixed three real-convention deviations in `WSCC9.svg`'s three transformers (T1/T2/T3), per a corpus sample the user pasted from `examples/sld/Examples.svg`: (1) each winding's connecting stub was a `<polyline>` with no `data-voltage`, missing both the real shape's `<path>` tag (real xsde2svg draws a straight lead as a `path d="M x y h/v N"`, not a multi-point polyline) and its own `data-voltage` (needed per-element here since a transformer spans two voltages, unlike most elements which get one shared value on their outer `<g>`); now every stub is a `<path>` carrying `data-voltage` matching its own stroke. (2) T2/T3's circles were wrapped in a pointless `<g transform="translate(...) rotate(0)">` sub-group; flattened to plain absolute `cx`/`cy` circles as direct children, matching the cited sample's own unrotated instance exactly (no inner group at all). (3) T1's `rotate(90,...)` transform lived on an inner sub-group wrapping only its two circles, leaving its two stub paths unrotated outside it; moved the transform to T1's own root `<g>` (matching the sample's rotated instance, `<g data-type="47" transform="rotate(90,1930,720)">`) and re-derived every child coordinate (both stub endpoints and both circle centers) in the pre-rotation frame via the inverse rotation, so the whole element rotates as one unit with no separate wrapper — verified by rendering that Bus4/Bus1 still connect exactly where they did before.
* 2026-09-08: Restored a formatting regression from a manual edit to `WSCC9.svg`: every `Digital device2:134` readout had lost the space between its numeric value and the `<tspan>` unit (e.g. "163 MW" had become "163MW"). Re-added the space to all 21 readouts, plus the same typo in the file's own header comment.
* 2026-09-08: Fixed a modeling gap in `WSCC9.svg`: each of the 12 breakers flanking a pure Overhead line (ids 1004-1015) had a single wire drawn straight through it, as if both terminals belonged to the same Overhead line — but a switch's two terminals are two distinct pieces of equipment, per the corpus sample the user pasted from `examples/sld/Examples.svg:155` (a bare `<polyline data-type="21">`, no wrapping `<g>`, no `data-name`). Split each of the six lines' polyline at both ends: the long run between its two breakers stays part of that `Overhead line:22`, while the short run out to each breaker's own bus became a new bare `Buswork:21` polyline (12 new elements, ids 351-362) — same voltage color either side, so the wire still reads as one continuous line, just correctly modeled as two pieces of equipment meeting at the switch instead of one line running through it.
* 2026-09-08: Found (via the user commenting out breaker 1017 and pointing out the transformer stayed connected to Bus 7 with no visual gap) that the same modeling gap existed for the six breakers sitting between a transformer's own winding and its adjacent bus (ids 1016-1021): each transformer's own lead ran straight through its breaker to the bus, so removing/opening the breaker left the wire fully intact underneath it. Applied the identical fix: shortened each transformer's lead (inside its own `<g data-type="47">`) to stop right at the breaker, and added 6 more bare `Buswork:21` polylines (ids 363-368) covering the remaining short run from breaker to bus. Verified by temporarily commenting breaker 1017 back out and rendering: a real gap now opens between T2's MV lead and Bus 7, where before the purple line stayed unbroken.
* 2026-09-08: Found (the user spotted it checking generator 101's own markup) that all 3 generators and, by the same pattern, all 3 loads had the identical modeling gap: each generator's own wire ran straight through its breaker all the way to its bus, and each load's own wire ran straight through its breaker back to the bus, instead of the bus-side run being separate `Buswork:21`. Fixed all 6: shortened each generator's wire (inside `<g data-type="173">`) to stop at its breaker, shortened each load's wire (inside `<g data-type="28">`) to start at its breaker, and added 6 more bare `Buswork:21` polylines (ids 369-374) covering the bus-side runs. Verified two ways: re-rendering after temporarily removing breaker 1001 confirmed a real gap opens next to G163, and a script checking every one of the file's 24 breakers' own leg-tip coordinates against every wire endpoint in the file confirmed all of them (T1's two, expressed in its pre-rotation local frame, checked by hand) now have a matching connection on both sides.
* 2026-09-08: Removed every `Buswork:21` stub in `WSCC9.svg` that was 5px or shorter (19 of the 24 just added: ids 351-354, 356, 358-368, 372-374) — per the user's screenshot showing them as barely-visible slivers between a breaker and its bus. Instead of a stub, each of those 19 breakers now moves so that side's leg tip lands exactly on the bus (no gap, no stub element needed there at all), with the wire or lead on its far side extended by the same distance the breaker moved so the far-side connection still lines up exactly — the six pure Overhead lines' far endpoints, the six transformer leads, and both generator/load wires all got the corresponding few-pixel extension. Only the 5 stubs actually longer than 5px (ids 355, 357, 369-371) remain as real `Buswork:21` elements. Verified three ways: a script confirming zero remaining `Buswork:21` elements are 5px or shorter, a second script confirming every moved breaker's bus-side leg tip now lies exactly on its bus's own drawn segment (not just near it), and a cropped re-render of the Bus 8 cluster (the specific case in the user's screenshot) showing all three breakers sitting flush against the bus with no visible stub.
